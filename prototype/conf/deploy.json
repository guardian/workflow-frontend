{
    "defaultStacks": ["workflow"],
    "packages": {
        "prototype": {
            "type": "autoscaling",
            "data": {
                "port": 9000,
                "bucket": "workflow-dist",
                "publicReadAcl": false
            }
        }
    },
    "recipes": {
        "default": {
            "depends": ["uploadArtifacts", "deploy"]
        },
        "deploy": {
            "actionsBeforeApp": ["prototype.deploy"]
        },
        "uploadArtifacts": {
            "actionsBeforeApp": ["prototype.uploadArtifacts"]
        }
    }
}
